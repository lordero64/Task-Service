# 🎯 Обзор
Executor Service — это отдельный Spring Boot микросервис для асинхронной обработки задач. Сервис принимает задачи через REST API, помещает их в очередь с приоритетом и обрабатывает в многопоточном режиме с использованием FixedThreadPool.

Основная цель: Асинхронная обработка и отправка сообщений в целевой сервис (Message Service) с гарантированной доставкой и retry-механизмом.

✨ Ключевые особенности
Особенность	Описание
- 🚀 Асинхронность	Клиент не ждёт выполнения задачи
- ⚡ Многопоточность	FixedThreadPool для параллельной обработки
- 📊 Приоритетная очередь	PriorityBlockingQueue с сортировкой задач
- 🔄 Retry-механизм	Автоматические повторные попытки при ошибках
- 📈 Мониторинг	REST API для получения статистики
- 🛡️ Отказоустойчивость	Graceful shutdown и обработка ошибок
- 📋 Функциональность

🎪 Типы поддерживаемых задач
1. DUMMY Task — Простая задача логирования
✅ Принимает текстовое сообщение

✅ Логирует в консоль с уровнем INFO

✅ Имитирует обработку (задержка 1 секунда)

✅ Поддерживает приоритеты (1-10)

2. REAL Task — Задача отправки данных
✅ Принимает данные для отправки

✅ Отправляет POST запрос в целевой сервис

✅ Поддерживает повторные попытки (retry)

✅ Экспоненциальная задержка между попытками

✅ Логирование успеха/ошибки


# 🏗️ Архитектура системы
Диаграмма потока данных

<img width="1280" height="3665" alt="deepseek_mermaid_20251202_7e702d" src="https://github.com/user-attachments/assets/07778483-07ee-486f-bcba-b4a4b49e734b" />

```
executor-service/
├── src/main/java/com/example/executorservice/
│   ├── ExecutorServiceApplication.java     # 📦 Главный класс
│   ├── config/
│   │   └── AppConfig.java                  # ⚙️ Конфигурация Spring
│   ├── controller/
│   │   └── TaskExecutorController.java     # 🌐 REST контроллер
│   ├── service/
│   │   └── TaskExecutorService.java        # 🧠 Основная логика
│   ├── task/
│   │   ├── Task.java                       # 📝 Интерфейс задачи
│   │   ├── DummyTask.java                  # 🎭 DUMMY задача
│   │   └── RealTask.java                   # 🚀 REAL задача
│   └── dto/
│       ├── TaskRequest.java                # 📄 Базовый DTO
│       ├── DummyTaskRequest.java           # 📄 DTO для DUMMY
│       └── RealTaskRequest.java            # 📄 DTO для REAL
├── src/main/resources/
│   └── application.yml                     # 🔧 Конфигурация
├── pom.xml                                 # 📦 Maven зависимости
└── README.md                               # 📚 Документация
```

# 🚀 Быстрый старт
📋 Предварительные требования
Java 21 или выше

Maven 3.8 или выше

Message Service запущен на порту 8080

# ⚡ Шаги запуска
1. Клонирование и сборка
```bash
git clone <repository-url>
cd executor-service
mvn clean package
```
2. Запуск сервиса
```bash
# Способ 1: Через Maven
mvn spring-boot:run

# Способ 2: Через JAR файл
java -jar target/executor-service-1.0.0.jar
```

3. Проверка работоспособности
```bash
curl http://localhost:8082/api/executor/health
```

Ожидаемый ответ:

```json
{
  "status": "UP",
  "message": "ExecutorService is running"
}
```

# 📡 API Endpoints
🔌 Доступные endpoints
| Метод |	Endpoint |	Описание
|-|--------|---|
|POST |	/tasks	| Отправка новой задачи
|GET	| /stats	| Получение статистики
|GET	| /health	| Проверка здоровья сервиса
